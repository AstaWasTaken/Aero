# Toolchain Placeholders

Add custom toolchain files here as needed, for example:

- cross-compilation presets
- CUDA-specific host compiler pinning
- CI-specific compiler/runtime selection

This scaffold does not require custom toolchains by default.